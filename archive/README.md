# Archive

Historical documentation retained for reference. Nothing here is required for
the primary ConvertAsset user workflow.

- `docs/legacy/` - superseded indexes and legacy mesh docs
- `docs/paper/` - paper/submission process records and old benchmark notes

Archived files may preserve pre-refactor path text as historical context. Use
[docs/index.md](../docs/index.md) for current documentation paths.

Rule: prefer Git history or tags for future backups; use this archive only when
the material remains useful to browse but should not appear in first-class
documentation navigation.
