# Shared Tables

Venue-neutral table sources for the ConvertAsset paper live here. Current table content remains embedded in `shared/sections/experiments.tex` until a later paper-editing pass extracts reusable tables.
