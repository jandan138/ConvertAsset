# CVPR/SynData4CV 2026 Version Status

Template provenance: `cvpr.sty`, `ieee_fullname.bst`, and `ieeenat_fullname.bst` are migrated from the previous single-venue workshop draft. The original wrapper and generated PDF are preserved under `archive/paper/cvpr26-workshop/`.

Readiness: historical-baseline

Local section overrides: none. This version inputs shared sections directly.

Known missing checks: this wrapper is retained for comparison only; it is not the active submission target. Rebuild only as a regression check after shared-section changes.
