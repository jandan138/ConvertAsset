# 2026-05-27 ACL Fig.3 Red-Material Diagnosis

## Scope

This record diagnoses the abnormal saturated red materials that were visible in
the pre-mitigation material-effect Figure 3 of:

```text
paper/venues/acl27/build/main.pdf
```

Latest superseding re-check on 2026-05-28: the ACL main paper now includes the
clean material-effect panel as Figure 3 on page 8. The current source figure
and rendered PDF page have zero strong-red pixels under the project spot check,
and `check_qualitative_clean_provenance.py` reports
`status=clean_material_effect_panel_ready` with four checked original-MDL logs
and zero MDL error signals. See
`docs/records/2026-05-28-acl-fig3-material-panel-red-recheck.md` for the
current diagnosis. The older entries below are retained as historical context
for stale pre-rerender panels and earlier PDFs where Figure 3 was still the
InternNav rollout panel.

That historical Figure 3 was sourced from:

```text
paper/shared/figures/fig_material_effect_baseline_qualitative.png
paper/shared/evidence/raw/material_effect_baseline/qualitative_render_manifest.json
```

Re-check on the current `paper/venues/acl27/build/main.pdf` shows that Figure 3
is now the selected InternNav rollout panel. As of the latest 2026-05-28 03:51
CST pre-upload gate, the ACL main PDF sources it from:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

The wider
`paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png`
derivative is retained for supplemental/rebuttal use, but it is not the current
main-text Figure 3.

The red marks in the current Figure 3 are trajectory/heading overlays. They are
not the old saturated red material fallback. The old material-effect panel remains
diagnosed here because it is still present as an internal figure asset and must
not be reintroduced without clean original-MDL rerenders.

The current InternNav Figure 3 source chain is:

```text
paper/venues/acl27/sections/limitations.tex
  -> figures/fig_internnav_rollout_0036_0066_column.png
  -> paper/shared/figures/gen_internnav_main_readable.py
  -> paper/shared/evidence/raw/internnav_vln_downstream/official_selected_qualitative_videos/stills/kujiale0036_0066/*.png
  -> /cpfs/user/zhuzihou/dev/InternNav/logs/official_kujiale0036_0066_*_video_selected6_20260525/video/*/*.mp4
```

The red marks are generated by InternNav visualization code, not by LaTeX:

```text
/cpfs/user/zhuzihou/dev/InternNav/internnav/evaluator/utils/common.py
```

`draw_action_pil()` uses `arrow_color=(255, 0, 0)` for the bottom-center action
arrow in the RGB view. `draw_trajectory()` draws the executed agent trajectory
with `r-` / `red` and the reference path with `g-` / `green` on the top-down map.
The current video-rerun config has `eval_settings.vis_output=True`, which is why
these overlays are intentionally present in the selected video stills.

The InternNav video logs checked for the current Figure 3 did not contain the
old MDL-resolution failure terms:

```text
KooPbr
KooPbr_maps
could not find module
Failed to create MDL shade node
C108
C120
```

Pixel checks also match overlay behavior. Earlier 0036/0066 rollout stills used
red trajectory/action overlays, but the current readable ACL Figure 3 source now
uses the revised purple overlay color. The current source image has no
strong-red pixels under the project spot check, while the retired material-effect
contact sheet still has a large saturated-red region in its `Original MDL`
cells.

## Current PDF Re-check

An earlier `paper/venues/acl27/build/main.pdf` re-check on 2026-05-27 reported a
10-page A4 PDF (`pdfinfo` file size `3363281` bytes). Its Figure 3 is still the
InternNav rollout panel, not the historical material-effect panel. The rendered
page-9 visual check shows the purple/green legend above the panel and the
caption immediately below it. The caption states that the overlays are for
orientation only: purple executed paths and action arrows, with overlay color
changed for readability, and green reference paths.

A later re-check of the current ACL build PDF created on 2026-05-27 15:52 CST
reports the same 10-page A4 PDF shape with file size `3313612` bytes. Rendering
page 9 at 180 DPI again shows Figure 3 as the InternNav rollout panel, with no
material-effect Original-MDL column in the PDF. The visible colored strokes are
the presentation-time purple agent overlay and green reference path described by
the in-figure legend and caption.

Latest re-check on the ACL build PDF created on 2026-05-27 17:07:59 CST reports
the same current state: `paper/venues/acl27/build/main.pdf` is a 10-page A4 PDF
1.5, 3,519,802 bytes, and page 9 renders Figure 3 as the selected InternNav
rollout panel sourced from
`paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png`.
There is no `Original MDL / ConvertAsset / NVIDIA` material-effect contact
sheet in the current PDF. The old red `Original MDL` cells remain present only
in the retired internal asset
`paper/shared/figures/fig_material_effect_baseline_qualitative.png`.

Follow-up re-check on the ACL build PDF created on 2026-05-27 17:45:59 CST
reports the same diagnosis with the latest staged candidate: 10 pages, A4, PDF
1.5, 3,471,360 bytes. Rendering page 9 at 180 DPI shows Figure 3 as the
selected official KuJiaLe InternNav rollout panel. The visible colored marks
are the purple agent action/trajectory overlay and green reference path stated
in the in-figure legend and caption. The ACL wrapper `main.tex` inputs only
`sections/intro`, `sections/related`, `sections/method`, `sections/results`,
`sections/discussion`, `sections/conclusion`, `sections/limitations`, and
`sections/ethical-considerations`; it does not input
`paper/shared/sections/experiments.tex`, where the retired material-effect
contact sheet is still kept for non-main diagnostic context.

Follow-up re-check on the ACL build PDF created on 2026-05-27 22:36:02 CST
reports the same diagnosis: `paper/venues/acl27/build/main.pdf` is a 10-page
A4 PDF 1.5, 3,480,649 bytes, with SHA-256
`b7add385dfe3dee48f738a4d684558d2b8dc6d5306409334aa16e04b11f020ce`.
Rendering page 9 at 170 DPI again shows Figure 3 as the selected InternNav
rollout panel sourced from
`paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png`
(SHA-256 `4d8881fd4833c32c039d8d5e72036570503a4bb6d08c1dec0d250fc4f398fe2e`).
The PDF does not contain the retired `Original MDL / ConvertAsset / NVIDIA`
material-effect contact sheet. A source-image pixel spot check counted `0`
strong-red pixels in the current Figure 3 PNG and `37,161` strong-red pixels
(`4.7502%`) in the retired
`paper/shared/figures/fig_material_effect_baseline_qualitative.png` asset.

The latest guarded provenance check for the retired material-effect contact
sheet still blocks it:

```text
status: blocked_material_effect_panel
selected_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 4
blockers: original_mdl_error_signal
```

The current claim-boundary check passes for the ACL wrapper, and the focused
tests that protect this boundary pass:

```text
python paper/venues/acl27/scripts/check_claim_boundaries.py
python -m pytest -q tests/test_material_effect_qualitative_clean_provenance.py \
  tests/test_acl_claim_boundaries.py tests/test_acl_integrity_fingerprint.py
```

Result: no unsafe figure violations in the ACL wrapper, and `12 passed`.

The underlying selected-video stills checked in the earlier pre-purple-overlay
pass contain strong-red pixels in both `original` and `nomdl` conditions. The
counts are small relative to the 512-by-1024 stills and are concentrated in
arrow/path strokes, for example:

| Still | Strong-red pixels | Ratio |
| --- | ---: | ---: |
| `kujiale0036_0066_597_597_original_start_000000.png` | 426 | 0.000813 |
| `kujiale0036_0066_597_597_original_end_000113.png` | 3695 | 0.007048 |
| `kujiale0036_0066_597_597_nomdl_start_000000.png` | 420 | 0.000801 |
| `kujiale0036_0066_597_597_nomdl_end_000101.png` | 2660 | 0.005074 |

An MDL-error-term scan over the current selected-video InternNav logs returned
no matches for `KooPbr`, `KooPbr_maps`, `could not find module`,
`Failed to create MDL shade node`, `C108`, or `C120`.

The latest source-still pixel check again finds source-red pixels in both
original and noMDL selected stills, with small ratios and map/arrow
concentration matching navigation overlays rather than material fallback. For
example, the current `0036/0066` main rows report source-red ratios from
`0.000761` to `0.007772` before the paper-panel recoloring step.

Latest user-facing re-check after the Figure 2 front-detail refresh reports the
same Figure 3 state: `paper/venues/acl27/build/main.pdf` is a 10-page A4 PDF
1.5, 3,493,873 bytes, and rendered page 9 shows Figure 3 as the selected
official KuJiaLe InternNav rollout panel. `pdftotext` confirms the
Figure 3 caption describes purple executed paths/action arrows and green
reference paths. The stale `Original MDL / ConvertAsset / NVIDIA` material
contact sheet is still present only as the internal asset
`paper/shared/figures/fig_material_effect_baseline_qualitative.png`, whose
`Original MDL` cells visibly contain saturated red fallback-looking materials.
The clean-provenance checker still blocks that internal contact sheet:

```text
status: blocked_material_effect_panel
selected_case_count: 4
complete_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 4
blockers: original_mdl_error_signal
```

Those four old `Original MDL` renders were generated from the May-2026 scratch
tree before a clean rerender was made. Their stderr logs contain `C120 could not
find module '::KooPbr'`, `C108`, `KooPbr_maps`, and `Failed to create MDL shade
node` signals. The currently mounted scratch asset files themselves now contain
the repaired relative import form, e.g. `using .::KooPbr import KooMtl;`. A
dry-run of `scripts/fix_mdl_absolute_imports.py` over the two selected scene
roots with `--follow-symlinks` found `1,566` candidate MDL files and `0`
remaining replacements. Therefore the visible old red material is stale render
evidence, not proof that the current MDL import fix is missing from those
mounted files. Reintroducing this material contact sheet requires fresh
original-MDL rerenders with clean logs, followed by regenerating the qualitative
panel and rerunning the clean-provenance guard.

Follow-up re-check after the Figure 2 front-angled refresh reports the same
Figure 3 state: `paper/venues/acl27/build/main.pdf` and the staged candidate are
identical 10-page A4 PDF 1.5 files of 3,514,678 bytes. Rendered page 9 still
shows Figure 3 as the selected official KuJiaLe InternNav rollout panel with
purple/green orientation overlays, not the retired material-effect contact
sheet.

Latest re-check for the user-reported Fig.3 concern on 2026-05-27 confirms the
same root cause. `pdfinfo paper/venues/acl27/build/main.pdf` reports a 10-page
A4 PDF 1.5 created at `2026-05-27 19:55:47 CST`, file size `3,515,110` bytes.
`pdftotext` places Figure 3 on page 9 with the caption "Supplemental
qualitative InternNav rollout panel", and `pdfimages -list` shows the page-9
image is the InternNav rollout panel. Rendering page 9 at 160 DPI shows the
visible color marks as purple/green navigation overlays. The current ACL
wrapper still does not include `fig_material_effect_baseline_qualitative.png`.

The retired internal material-effect contact sheet still visibly contains
saturated red `Original MDL` cells, but the clean-provenance guard blocks it:
`status=blocked_material_effect_panel`, `selected_case_count=4`,
`checked_original_mdl_log_count=4`, and
`original_mdl_error_signal_count=4`. A spot check of a log-referenced scratch
MDL file now shows repaired imports such as `using .::KooPbr import KooMtl;`
and no remaining absolute `import ::KooPbr` form in that file. The May-26 repair
report records `1,566` changed MDL files, `3,683` import replacements, and `0`
errors. Therefore the observed red material in the retired contact sheet is
stale pre-rerender evidence, not evidence that the current mounted MDL files
missed the import repair. Focused guard tests passed:
`tests/test_material_effect_qualitative_clean_provenance.py`,
`tests/test_acl_claim_boundaries.py`, and
`tests/test_fix_mdl_absolute_imports.py` reported `14 passed`.

After the subsequent Figure 2 proxy-sublabel precision pass, the refreshed full
pre-upload gate again stages the current ACL PDF without reintroducing the
retired material-effect contact sheet. The build and staged PDFs are identical
10-page A4 PDF 1.5 files of `3,515,110` bytes with SHA-256
`2800402b662904faf83862cd1f5fd1374e9d81fa6bdd8768f72e0a59458fa794`, and the
current Figure 3 remains the selected InternNav rollout panel.

Latest re-check on 2026-05-27 after a page-4 typography pass again confirms the
same diagnosis on the exact staged packet. `run_preupload_gate.py` passed with
74 focused tests and staged a 10-page A4 PDF 1.5 file of `3,513,952` bytes; the
build and staged PDFs are identical with SHA-256
`81acd09574094b915097cc23fa8687f7822ed5dcd976f9d0ea9587faca2a8177`.
`pdftotext -f 9 -l 9` places Figure 3 on page 9 with the caption
"Supplemental qualitative InternNav rollout panel", and `pdfimages -list`
shows only three embedded images in the PDF: Figure 1 on page 2, Figure 2 on
page 6, and the InternNav Figure 3 on page 9. Rendering page 9 at 160 DPI shows
purple/green navigation overlays, not red fallback material. Pixel checks on
the current Figure 3 source
`paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png`
found `0` strong-red pixels under the `r>180,g<80,b<80` test; the retired
`fig_material_effect_baseline_qualitative.png` still contains strong red
pixels and remains an internal blocked artifact.

## Latest 2026-05-28 Re-check

Re-checking the exact file reported by the user:

```text
paper/venues/acl27/build/main.pdf
```

shows the current ACL build is an 11-page A4 PDF 1.5, created at
`2026-05-28 06:34:38 CST`, with size `3,072,456` bytes and SHA-256:

```text
5028f38ebc5ac398e3e99980ca00d909abfd4a8e6a61bcf2fb148b6ff5e3857c
```

`pdftotext` places Figure 3 on page 9 with the caption
`Selected qualitative InternNav rollout panel`. `pdfimages -list` shows the
page-9 embedded image is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

The image extracted from the PDF is pixel-identical to that source PNG
(`max_abs_diff=0`, `nonzero_pixels=0`). The source PNG SHA-256 is:

```text
7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Pixel checks on the current PDF page render and on the current source PNG found
no saturated red material signal:

| Artifact | Strong-red pixels | Red/orange pixels |
| --- | ---: | ---: |
| `tmp/fig3_diag_20260528/page-09.png` | 0 | 0 |
| `paper/shared/figures/fig_internnav_rollout_0036_0066_column.png` | 0 | 0 |

The source video stills do contain red pixels, but those are InternNav
visualization overlays before the paper-panel recoloring step. The current
generator recolors those source-red action/trajectory pixels to purple without
editing the source stills. For the Figure 3 column panel, the generator reports:

| Source still | Source-red overlay pixels recolored |
| --- | ---: |
| `kujiale0036_0066_597_597_original_start_000000.png` | 475 |
| `kujiale0036_0066_597_597_original_end_000113.png` | 4,177 |
| `kujiale0036_0066_597_597_nomdl_start_000000.png` | 461 |
| `kujiale0036_0066_597_597_nomdl_end_000101.png` | 3,003 |

This source-red pattern appears in both `original` and `nomdl` conditions, which
matches navigation overlays rather than an original-MDL material fallback.
Scanning the current selected-video InternNav log roots for `KooPbr`,
`KooPbr_maps`, `could not find module`, `Failed to create MDL shade node`,
`C108`, and `C120` returned no matches.

The material-effect contact sheet is no longer the current Figure 3. It has also
since been clean-rerendered: the current
`paper/shared/figures/fig_material_effect_baseline_qualitative.png` has `0`
strong-red pixels under the same spot check, and
`check_qualitative_clean_provenance.py` now reports:

```text
status: clean_material_effect_panel_ready
selected_case_count: 4
complete_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 0
blockers: []
```

Conclusion: the red issue previously observed in old material-effect panels was
a stale-render/provenance problem, not a missing MDL import repair in the current
mounted assets. The exact current ACL `main.pdf` Figure 3 does not contain the
old red material panel and does not contain saturated red pixels after the
InternNav overlay recoloring step.
The previous 73-test proxy-sublabel run is superseded by this 74-test recheck.

## Current InternNav Overlay Color Polish

To further reduce the chance that readers mistake navigation overlays for
material fallback, `paper/shared/figures/gen_internnav_main_readable.py` now
recolors only the source-red InternNav agent overlay pixels while composing the
paper panel. The source stills remain unchanged under
`paper/shared/evidence/raw/internnav_vln_downstream/official_selected_qualitative_videos/stills/`.
The generated main-text panel reports:

```text
source_overlay_color: red
display_overlay_color: purple
total_source_red_overlay_pixels_recolored: 23118
```

The column panel reports `8116` recolored source-red overlay pixels. This is a
presentation-only overlay-color change for readability, not a material edit and
not a rerendered evidence condition.

## Current InternNav Figure 3 Overlay Hardening

The current ACL Figure 3 is not a material-effect comparison panel. It is a
selected official KuJiaLe InternNav rollout panel. To make this unambiguous in
the paper, the figure generator now embeds a purple/green overlay legend
directly in both generated InternNav panels:

```text
paper/shared/figures/gen_internnav_main_readable.py
paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

The generated report records:

```text
purple -> agent action arrow and executed trajectory
green -> reference path
```

The ACL Figure 3 caption in `paper/venues/acl27/sections/limitations.tex` now
also states that the overlays are for orientation only and that the agent
overlay color was changed for readability. This keeps the selected video-still
panel from being mistaken for a material appearance claim, and keeps
quantitative embodied-data claims tied to the 99-episode paired run and
official-scene load/render checks.

Local rendered-PDF visual review of page 9 after regeneration shows the legend
visible in the figure and the caption visible immediately below it. A first
legend layout pass had the red and green labels too close together; the final
generator advances legend entries by measured text width rather than a fixed
offset.

Fresh verification after the overlay hardening:

- `python -m pytest -q tests/test_internnav_rollout_figure.py`:
  `2 passed`.
- Focused Fig.3/material/layout/fingerprint suite:
  `21 passed`.
- `python paper/venues/acl27/scripts/check_claim_boundaries.py`: `ok=true`,
  with no unsafe figure violations.
- `python paper/venues/acl27/scripts/check_integrity_fingerprint.py`: `ok=true`,
  `source_count=53` after refreshing the intended Fig.3 source changes.
- `python paper/venues/acl27/scripts/run_preupload_gate.py`: `ok=true`; after
  the purple overlay-color polish, it rebuilt and staged a 10-page A4 PDF 1.5
  packet, `main.pdf` size `3363508` bytes, with 70 focused tests passing.

## Current Page-8 Layout Polish

A follow-up rendered-PDF review after the Fig.3 mitigation found an unrelated
layout problem: the official-scene performance Table 6 could still behave like a
single-column float and make the transition into Limitations look awkward.
This was not a material-rendering issue, but it affected the same PDF review
pass.

The fix keeps the same evidence numbers and changes only presentation:

- `paper/shared/tables/tab_official_scene_performance_summary.tex` now uses a
  two-column `table*` compact aggregate layout.
- `paper/shared/evidence/experiments/10_official_scene_submission_closure/build_submission_closure_package.py`
  now regenerates that same `table*` layout.
- `paper/venues/acl27/sections/conclusion.tex` is shortened to a single
  claim-boundary paragraph so Conclusion no longer leaves a distracting orphan
  before Limitations.

Fresh verification after this layout polish:

- `python -m pytest -q tests/test_official_scene_submission_closure.py tests/test_paper_layout.py tests/test_acl_preupload_gate.py`:
  `31 passed`.
- `python paper/venues/acl27/scripts/check_integrity_fingerprint.py`: `ok=true`,
  `source_count=53` after refreshing the intended source changes.
- `python paper/venues/acl27/scripts/run_preupload_gate.py`: `ok=true`; it
  rebuilt and staged a 10-page A4 PDF 1.5 packet, `main.pdf` size `3363281`
  bytes, with 70 focused tests passing.
- Local rendered-PDF visual review of pages 7--9 confirmed that Table 6,
  Limitations, and the selected InternNav Figure 3 are readable without
  overlap.

## Historical Material-Effect Panel Finding

The historical material-effect red material was already present in that old
Figure 3 source PNG, so it was not a LaTeX/PDF compression artifact. The source
contact sheet used original-condition GRScenes render PNGs that were produced
before the May 26 MDL import repair.

The selected Figure 3 original-MDL rows point to these older render images:

| Pair | Target | Original image |
| --- | --- | --- |
| `c27086f557d316584264.zoom_018` | bottle | `paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_renders/MV7J6NIKTKJZ2AABAAAAADA8_usd/c27086f557d316584264/zoom_018/original/original_0000.png` |
| `f35ef3d86c42446b7ddf.zoom_018` | clock | `paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_renders/MV7J6NIKTKJZ2AABAAAAADQ8_usd/f35ef3d86c42446b7ddf/zoom_018/original/original_0000.png` |
| `e2ec085d524d5df4455d.zoom_020` | cup | `paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_renders/MV7J6NIKTKJZ2AABAAAAADQ8_usd/e2ec085d524d5df4455d/zoom_020/original/original_0000.png` |
| `47aa36277a54f6ca90cc.zoom_018` | backpack | `paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_renders/MV7J6NIKTKJZ2AABAAAAADQ8_usd/47aa36277a54f6ca90cc/zoom_018/original/original_0000.png` |

Their paired render reports record MDL/KooPbr error signal in the original
condition and zero signal in the converted condition:

| Pair | Original MDL error signal | Converted MDL error signal |
| --- | ---: | ---: |
| `c27086f557d316584264.zoom_018` | 1556 | 0 |
| `f35ef3d86c42446b7ddf.zoom_018` | 988 | 0 |
| `e2ec085d524d5df4455d.zoom_020` | 988 | 0 |
| `47aa36277a54f6ca90cc.zoom_018` | 988 | 0 |

The corresponding original stderr logs include MDL compiler errors such as:

```text
C120 could not find module '::KooPbr' in module path
C120 could not find module '::KooPbr_maps' in module path
Failed to create MDL shade node
```

## Why The Previous Fix Did Not Clean Figure 3

The May 26 repair did fix the scratch asset tree in place:

```text
paper/shared/evidence/raw/grscene_vlm_grounding/mdl_import_fix_apply_20260526.json
```

That report records `1566` changed MDL files, `3683` import replacements, and
`0` errors. Spot checks on the Figure 3 log-referenced `.mdl` files now show
relative imports such as:

```text
using .::KooPbr import KooMtl;
using .::KooPbr_maps import KooPbr_bitmap;
```

and no remaining `import ::KooPbr` / `import ::KooPbr_maps` lines in the scanned
scratch root.

However, the historical material-effect Figure 3 did not use newly rerendered
original-MDL images.
It reassembles already-existing PNG files from the older
`retake_zoom_renders/` directory. Regenerating the contact sheet on May 26 only
copied those stale PNGs into a fresh contact sheet; it did not rerender the
original-MDL cells after the asset repair.

## Paper Impact

The historical material-effect Figure 3 is not upload-safe as a clean
"Original MDL vs ConvertAsset vs NVIDIA" material-effect qualitative panel. In
that form, the original column can be read as material appearance, but the
provenance says it includes MDL-resolution failure artifacts from the old render
pass.

This does not invalidate the May 26 MDL import repair. It means the repair has
not yet been propagated into the Figure 3 image evidence.

## ACL Main-Paper Mitigation

The ACL main results section now removes
`figures/fig_material_effect_baseline_qualitative.png` and keeps the
material-effect discussion bounded to `tab_material_effect_risk_matrix`. The
claim-boundary checker now rejects the stale material-effect panel if it is
reintroduced before the original-MDL cells are rerendered or replaced with
clean-provenance cases.

After this mitigation, the refreshed ACL PDF no longer uses the old
material-effect contact sheet as Figure 3. The current Figure 3 in the staged
candidate is the selected InternNav rollout panel; the material-effect section
is intentionally table-only in the main paper.

## Required Fix Path

Before the material-effect panel is used as main-paper evidence, use one of
these routes:

1. Rerender the four original-MDL Figure 3 source images from the repaired
   scratch asset tree, require zero `KooPbr`, `KooPbr_maps`, `could not find
   module`, and `Failed to create MDL shade node` signals in the new logs, then
   regenerate `fig_material_effect_baseline_qualitative.png`.
2. Replace Figure 3 with material-effect cases whose original/noMDL/NVIDIA
   render records already have clean provenance.
3. Remove the panel from the main ACL paper and keep the current image only as
   internal diagnostic evidence until clean original-MDL renders are available.

## Latest Re-check: 2026-05-27 23:08 Build

The current build at:

```text
paper/venues/acl27/build/main.pdf
```

is a 12-page A4 PDF 1.5 with SHA-256
`f099bfe15e16e234748324a6717604b5cf4a826918f7e66add6c5cc5c5cb504c`.
`pdftotext` places Figure 3 on page 10 with the caption
`Supplemental qualitative InternNav rollout panel`; it is sourced from:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_main3_readable.png
```

This current Figure 3 is not the retired `Original MDL / ConvertAsset /
NVIDIA` material-effect contact sheet. Pixel checks confirm the distinction:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 10 | 0 | 0.000000 | No strong-red material/overlay pixels in current Fig.3 page render. |
| Current Fig.3 source PNG | 0 | 0.000000 | InternNav source-red overlays are recolored to purple in the paper panel. |
| Retired material-effect contact sheet | 48,593 | 0.062115 | Old Original-MDL cells still contain saturated red fallback-looking render evidence. |
| Current Figure 2 render-evidence PNG | 0 | 0.000000 | No strong-red material issue in the current main render-evidence figure. |

The provenance checker still blocks the retired material-effect contact sheet:

```text
status: blocked_material_effect_panel
selected_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 4
blockers: original_mdl_error_signal
```

The four blocked Original-MDL source renders are stale May-22 render outputs.
Their stderr logs contain `C120 could not find module '::KooPbr'`, `C108`,
`KooPbr_maps`, and `Failed to create MDL shade node`. The current mounted MDL
files under the same two scene roots already use repaired relative imports such
as `using .::KooPbr import KooMtl;`. A dry run over those roots with
`scripts/fix_mdl_absolute_imports.py --dry-run --follow-symlinks` scanned
1,566 candidate MDL files and reported:

```text
changed_files: 0
total_replacements: 0
errors: 0
```

Root-cause conclusion: the user-visible red-material problem was fixed at the
MDL-file level, but the retired material-effect contact sheet reused stale
pre-fix Original-MDL PNGs. It must not be reintroduced to the ACL main PDF until
the four Original-MDL cells are rerendered from the repaired assets with clean
logs, or replaced by clean-provenance cases.

## Latest Re-check: 2026-05-27 23:52 Build

After the subsequent single-column Figure 3 layout pass, the current ACL build
at:

```text
paper/venues/acl27/build/main.pdf
```

is an 11-page A4 PDF 1.5, `3,114,437` bytes, with SHA-256
`7df61ee4eb65ca256a35cd45a19ae53ed42baff390c78049cbf4f6bae617e5f3`.
`pdfimages -list` shows exactly three embedded raster figures in the main PDF:
Figure 1 on page 2, Figure 2 on page 6, and Figure 3 on page 9. The page-9
image dimensions are `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

`pdftotext -f 9 -l 10` places Figure 3 with the caption
`Supplemental qualitative InternNav rollout panel`; it does not contain an
`Original MDL / ConvertAsset / NVIDIA` material-effect contact sheet. Rendering
page 9 at 180 DPI confirms visually that the colored marks are purple/green
navigation overlays inside the selected InternNav panel.

Pixel spot checks on the exact current artifacts report:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 9 | 0 | 0.000000 | No strong-red material/overlay pixels in the current Figure 3 page render. |
| Current Figure 3 source column PNG | 0 | 0.000000 | Source-red InternNav overlays were recolored to purple for the paper panel. |
| Retained full-width InternNav derivative | 0 | 0.000000 | Also clean with respect to the strong-red spot check. |
| Retired material-effect contact sheet | 37,161 | 0.047502 | Old `Original MDL` cells still contain saturated red fallback-looking evidence. |

Therefore, if an opened PDF still shows a Figure 3 with an `Original MDL`
column and saturated red materials, it is not the current ACL build. It is an
older build or an obsolete copy of the retired internal asset before the
2026-05-28 clean rerender described below.

## 2026-05-28 Clean Rerender Follow-up

The four selected covered-bin material-effect cases were rerendered from the
repaired MDL asset tree using the existing zoom camera stages:

```bash
python paper/shared/evidence/experiments/06_grscenes_vlm_grounding/run_paired_render_smoke.py \
  --render-manifest paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_render_manifest.json \
  --pair-id <selected_pair_id> \
  --out paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_paired_render_reports/<selected_pair_id>.clean_rerender_20260528.json \
  --log-dir paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_render_logs \
  --timeout-seconds 1200
```

Selected pair ids:

```text
c27086f557d316584264.zoom_018
f35ef3d86c42446b7ddf.zoom_018
e2ec085d524d5df4455d.zoom_020
47aa36277a54f6ca90cc.zoom_018
```

All four rerender reports have original/noMDL exit code 0, both images present,
and `original_mdl_error_signal=0`. Strong-red pixel spot checks on the four
new Original MDL cells also report 0 strong-red pixels. The material-effect
qualitative manifest and contact sheet were regenerated:

```bash
python paper/shared/evidence/experiments/08_material_effect_baseline/build_qualitative_render_manifest.py
python paper/shared/figures/gen_material_effect_qualitative.py
python paper/shared/evidence/experiments/08_material_effect_baseline/check_qualitative_clean_provenance.py --report-only
```

The clean-provenance gate now reports:

```text
status: clean_material_effect_panel_ready
selected_case_count: 4
complete_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 0
blockers: []
```

Local pure-visual review of the regenerated
`paper/shared/figures/fig_material_effect_baseline_qualitative.png` finds no red
material fallback in the Original MDL cells. The NVIDIA rows remain visibly
lower fidelity in places such as the low-contrast clock and washed backpack
target, so the panel supports selected qualitative inspection only. It does not
change the paper boundary: covered bins remain bounded selected evidence,
clearcoat remains a selected NVIDIA failure case, and procedural texture remains
a limitation case.

## Latest Re-check: 2026-05-28 02:27 Build

The current build at:

```text
paper/venues/acl27/build/main.pdf
```

is an 11-page A4 PDF 1.5, `3,090,166` bytes, with SHA-256
`9c0fa34c200e0d09cf4b3f96061325eb44d77a66ec7afad0bb917f1258358605`.
The staged candidate PDF has the same size and hash.

`pdftotext` places Figure 3 on page 9 with the caption
`Supplemental qualitative InternNav rollout panel`. `pdfimages -list` shows
three embedded raster figures in the main PDF: Figure 1 on page 2, Figure 2 on
page 6, and Figure 3 on page 9. The page-9 image is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

This current Figure 3 is not an `Original MDL / ConvertAsset / NVIDIA`
material-effect panel. Rendering page 9 at 180 DPI shows the selected InternNav
rollout panel with purple agent overlays and green reference paths, matching
the in-figure legend and caption. A pixel spot check reports:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 9 | 0 | 0.000000 | No strong-red pixels in the current Figure 3 page render. |
| Current Figure 3 source column PNG | 0 | 0.000000 | InternNav source-red overlays are recolored to purple in the paper panel. |
| Regenerated material-effect contact sheet | 0 | 0.000000 | The 2026-05-28 clean rerender removed the stale red Original-MDL fallback evidence. |

An MDL-error-term scan over the current selected-video InternNav logs returned
no matches for `KooPbr`, `KooPbr_maps`, `could not find module`,
`Failed to create MDL shade node`, `C108`, or `C120`. Therefore, if a viewer
still shows red material in a Figure 3 with an `Original MDL` column, it is a
stale PDF/viewer cache or an older artifact, not the current
`paper/venues/acl27/build/main.pdf`.

## Previous Re-check: 2026-05-28 03:21 Build

The current ACL build at:

```text
paper/venues/acl27/build/main.pdf
```

is an 11-page A4 PDF 1.5, `3,090,159` bytes, with SHA-256
`bafb5b48ef6eb15754eaf2fef9139a4f6cba9d0b34d3816c0753f1e7f0323565`.
The staged candidate PDF at
`paper/submissions/acl27_arr_candidate_20260526/main.pdf` has the same size and
hash.

`pdftotext` places Figure 3 on page 9 with the caption
`Supplemental qualitative InternNav rollout panel`. `pdfimages -list` shows
three embedded raster figures in the main PDF: Figure 1 on page 2, Figure 2 on
page 6, and Figure 3 on page 9. The page-9 image is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

The current Figure 3 therefore is still the selected InternNav rollout panel,
not an `Original MDL / ConvertAsset / NVIDIA` material-effect comparison panel.
Local rendered-PDF visual review of page 9 shows only purple agent overlays and
green reference paths, as described by the in-figure legend and caption. The
03:21 clean rebuild changed the PDF hash through build metadata, but page-raster
hashes are unchanged from the 03:07 visual-review pass. Pixel spot checks
report:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 6 | 0 | 0.000000 | Figure 2 render-evidence page has no strong-red material pixels. |
| Rendered current PDF page 9 | 0 | 0.000000 | Current Figure 3 page render has no strong-red material/overlay pixels. |
| Current Figure 2 source PNG | 0 | 0.000000 | `fig_render_scene_evidence_wide.png` has no strong-red pixels. |
| Current Figure 3 source column PNG | 0 | 0.000000 | InternNav source-red overlays remain recolored to purple in the paper panel. |
| Regenerated material-effect contact sheet | 0 | 0.000000 | The clean rerendered `fig_material_effect_baseline_qualitative.png` no longer contains the stale red Original-MDL fallback evidence. |

The material-effect clean-provenance guard also remains clean:

```text
status: clean_material_effect_panel_ready
selected_case_count: 4
complete_case_count: 4
checked_original_mdl_log_count: 4
original_mdl_error_signal_count: 0
blockers: []
```

The diagnosis is unchanged but more specific for the current packet: a viewer
showing red material in a figure with an `Original MDL` column is not showing
the current ACL main PDF. It is showing an older PDF, a stale PDF-viewer cache,
or a retired material-effect artifact from before the clean original-MDL
rerender.

## Previous Re-check: 2026-05-28 03:33 Build

The current ACL build at:

```text
paper/venues/acl27/build/main.pdf
```

is an 11-page A4 PDF 1.5, `3,094,231` bytes, with SHA-256
`62e924a1bda02a7423399d180587df800cfdf0d4e93bd16e5be88f370a0aece3`.
`pdfimages -list` shows exactly three embedded raster figures in the main PDF:
Figure 1 on page 2, Figure 2 on page 6, and Figure 3 on page 10. The page-10
image is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

`pdftotext -layout -f 8 -l 10` places Figure 3 with the caption
`Supplemental qualitative InternNav rollout panel`; the current PDF does not
contain a Figure 3 with `Original MDL / ConvertAsset / NVIDIA` material-effect
columns. The embedded page-10 image extracted by `pdfimages -png` is
pixel-identical to `fig_internnav_rollout_0036_0066_column.png`.

Pixel spot checks on the exact current artifacts report:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 10 | 0 | 0.000000 | No strong-red pixels in the current Figure 3 page render. |
| Extracted embedded Figure 3 image | 0 | 0.000000 | The image embedded in the PDF has no strong-red pixels. |
| Current Figure 3 source column PNG | 0 | 0.000000 | InternNav source-red overlays remain recolored to purple in the paper panel. |
| Regenerated material-effect contact sheet | 0 | 0.000000 | The clean rerendered selected material-effect panel remains free of stale red Original-MDL fallback evidence. |

The clean-provenance guard still reports
`status=clean_material_effect_panel_ready` and
`original_mdl_error_signal_count=0`.

Root-cause conclusion for the current user report: the MDL import repair and the
2026-05-28 clean rerender did propagate into the material-effect evidence, but
the current ACL main PDF no longer uses that material-effect contact sheet as
Figure 3. If a viewer shows a red `Original MDL` column while opening this path,
it is almost certainly showing an older PDF render/cache or another artifact
with the retired material-effect layout, not the bytes currently present at
`paper/venues/acl27/build/main.pdf`.

## Prior Re-check: 2026-05-28 03:51 Gate

After the VLM-backend provenance edit and the OpenReview page-anchor sync, the
full pre-upload gate was rerun:

```bash
python paper/venues/acl27/scripts/run_preupload_gate.py
```

Result: pass, with `83 passed` in the focused pytest step. The current ACL
build and staged packet are byte-identical:

```text
paper/venues/acl27/build/main.pdf
paper/submissions/acl27_arr_candidate_20260526/main.pdf
sha256=1331675b11801dca013866facc005390c54e3f8e77234fb62d23b318fa137312
size=3,094,113 bytes
pages=11
pdf=1.5 A4
created=2026-05-28 03:51:54 CST
```

`pdfimages -list` still shows exactly three embedded raster figures in the main
PDF: Figure 1 on page 2, Figure 2 on page 6, and Figure 3 on page 9. The page-9
image is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
```

`pdftotext -layout -f 9 -l 9` places `Limitations`, Figure 3, Ethical
Considerations, and the start of References on page 9. The Figure 3 caption is
`Supplemental qualitative InternNav rollout panel`; it describes purple
executed paths/action arrows and green reference paths. There is no Figure 3
with `Original MDL / ConvertAsset / NVIDIA` columns in the current ACL main
PDF.

Final pixel spot checks on the exact current artifacts report:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 9 | 0 | 0.000000 | No strong-red pixels in the current Figure 3 page render. |
| Current Figure 3 source column PNG | 0 | 0.000000 | InternNav source-red overlays remain recolored to purple in the paper panel. |
| Regenerated material-effect contact sheet | 0 | 0.000000 | The clean rerendered selected material-effect panel remains free of stale red Original-MDL fallback evidence. |

The current material-effect clean-provenance guard remains clean:

```text
status=clean_material_effect_panel_ready
selected_case_count=4
complete_case_count=4
checked_original_mdl_log_count=4
original_mdl_error_signal_count=0
blockers=[]
```

Current conclusion: the original MDL import issue was fixed and the selected
material-effect cells have since been rerendered with clean logs. The current
ACL Figure 3 is not that material-effect panel at all; it is the InternNav
rollout panel. A red `Original MDL` Figure 3 seen at the PDF path therefore
indicates an older PDF, a PDF-viewer cache, or a retired internal artifact, not
the current bytes of `paper/venues/acl27/build/main.pdf`.

## Follow-up Re-check After Method Typography Pass

After the Method checkpoint-ID typography pass, the full pre-upload gate was
rerun on 2026-05-28:

```bash
python paper/venues/acl27/scripts/run_preupload_gate.py
```

The gate passed with 83 focused tests and restaged the current candidate. The
build and staged PDFs are byte-identical 11-page A4 PDF 1.5 files of
`3,094,113` bytes with SHA-256
`221427f211d1bd874315122aaa1404093eaaa76583fdd5ad6e2f2f6f16306ec6`;
`pdfinfo` reports creation/modification time `2026-05-28 04:10:45 CST`.
`pdfimages -list` still shows exactly three embedded raster figures: Figure 1
on page 2, Figure 2 on page 6, and Figure 3 on page 9.

Rendered-PDF and source-figure red-pixel checks remain clean:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 6 | 0 | 0.000000 | Figure 2 render-evidence page has no strong-red material pixels. |
| Rendered current PDF page 9 | 0 | 0.000000 | Current Figure 3 page render has no strong-red material/overlay pixels. |
| Current Figure 3 source column PNG | 0 | 0.000000 | The selected InternNav rollout source figure remains free of source-red overlays. |

At that checkpoint, `pdftotext -layout -f 9 -l 9` still identified Figure 3 as
`Supplemental qualitative InternNav rollout panel`. Therefore the PDF at that
point still did not contain the retired `Original MDL / ConvertAsset / NVIDIA`
material-effect contact sheet as Figure 3.

## Follow-up Re-check After Conclusion Polish

After moving the repeated practical reporting-pattern takeaway from Discussion
into Conclusion, the full pre-upload gate was rerun again on 2026-05-28. It
passed with 83 focused tests and staged byte-identical build/submission PDFs at
that checkpoint:
11-page A4 PDF 1.5, `3,093,559` bytes, SHA-256
`b5d52b62544f35081332cca8c3821a4ed88420050483884d1b529faf1fbdce28`, created
`2026-05-28 04:35:49 CST`.

The page-level rendered visual check still places Figure 3 on page 9 as the
selected official KuJiaLe InternNav rollout panel. Strong-red checks remain
zero on rendered page 6 and page 9:

| Artifact | Strong-red pixels | Strong-red ratio | Interpretation |
| --- | ---: | ---: | --- |
| Rendered current PDF page 6 | 0 | 0.000000 | Figure 2 render-evidence page has no strong-red material pixels. |
| Rendered current PDF page 9 | 0 | 0.000000 | Current Figure 3 page render has no strong-red material/overlay pixels. |

## Verification Commands

Commands used for this diagnosis:

```bash
pdftotext -layout paper/venues/acl27/build/main.pdf - \
  | rg -n "Figure 3|Supplemental qualitative InternNav rollout|material-effect"

rg -n "fig_internnav_rollout_0036_0066_main3_readable|internnav-rollout" \
  paper/venues/acl27 paper/shared -S

rg -n "KooPbr|KooPbr_maps|could not find module|Failed to create MDL shade node|C120|C108" \
  /cpfs/user/zhuzihou/assets/convertasset_research/experiments/internnav/official/internnav_official_val_unseen_20260525/video_rerun/selected_0036_0066_20260525/logs/original_video_selected6_run.log \
  /cpfs/user/zhuzihou/assets/convertasset_research/experiments/internnav/official/internnav_official_val_unseen_20260525/logs/official_kujiale0036_33_original_run.log \
  /cpfs/user/zhuzihou/assets/convertasset_research/experiments/internnav/official/internnav_official_val_unseen_20260525/logs/official_kujiale0066_33_original_run.log

python - <<'PY'
from PIL import Image
from pathlib import Path
root=Path('paper/shared/evidence/raw/internnav_vln_downstream/official_selected_qualitative_videos/stills/kujiale0036_0066')
for key in ['597_597','898_898','919_919']:
    print('CASE', key)
    for condition in ['original','nomdl']:
        for label in ['start','end']:
            p=next(root.glob(f'kujiale0036_0066_{key}_{condition}_{label}_*.png'))
            im=Image.open(p).convert('RGB')
            w,h=im.size
            for name,x0,x1 in [('ego',0,w//2),('map',w//2,w)]:
                red=sum(
                    1
                    for r,g,b in im.crop((x0,0,x1,h)).getdata()
                    if r>180 and g<80 and b<80
                )
                print(condition, label, name, red)
PY

python - <<'PY'
import json
from pathlib import Path
m=json.loads(Path('paper/shared/evidence/raw/material_effect_baseline/qualitative_render_manifest.json').read_text())
for r in m['records']:
    if r['condition']=='original_MDL':
        print(r['sample_id'], r['image']['path'])
PY

rg -n -m 8 "C108|C120|Failed to create MDL shade node|KooPbr|KooPbr_maps" \
  paper/shared/evidence/raw/grscene_vlm_grounding/retake_zoom_render_logs/*/*.original.stderr.txt

rg -n "import ::KooPbr|import ::KooPbr_maps" \
  /cpfs/user/zhuzihou/assets/zzh-grscenes_nomdl_full_work_20260521/scenes/GRScenes-100/home_scenes \
  -g '*.mdl'

rg -n "using \\.::KooPbr|using \\.::KooPbr_maps" \
  /cpfs/user/zhuzihou/assets/zzh-grscenes_nomdl_full_work_20260521/scenes/GRScenes-100/home_scenes \
  -g '*.mdl' | wc -l

python -m pytest -q tests/test_internnav_rollout_figure.py

python paper/shared/figures/gen_internnav_main_readable.py

make -C paper acl27

mkdir -p tmp/acl27_visual_review
pdftoppm -png -f 9 -singlefile \
  paper/venues/acl27/build/main.pdf \
  tmp/acl27_visual_review/acl27_page9
```

## User-Reported Recheck: 2026-05-28 Current Build Path

The user re-opened:

```text
paper/venues/acl27/build/main.pdf
```

and asked whether abnormal red material in current Figure 3 means the earlier
MDL import repair failed. The exact bytes currently at that path are the
post-Figure-3-minipage candidate:

```text
sha256=5028f38ebc5ac398e3e99980ca00d909abfd4a8e6a61bcf2fb148b6ff5e3857c
size=3,072,456 bytes
pages=11
created=2026-05-28 06:34:38 CST
```

Direct PDF inspection confirms that current Figure 3 is the InternNav rollout
panel on page 9, not the retired material-effect contact sheet:

```text
Figure 3: Selected qualitative InternNav rollout panel.
```

`pdfimages -list` reports exactly three embedded raster figures in the main PDF:
Figure 1 on page 2, Figure 2 on page 6, and Figure 3 on page 9. The page-9 image
is `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Current pixel checks:

| Artifact | Strong-red pixels | Red-ish pixels | Purple-overlay-like pixels | Interpretation |
| --- | ---: | ---: | ---: | --- |
| Rendered current PDF page 9 | 0 | 0 | 15,440 | Figure 3 contains purple trajectory/action overlays, not red material fallback. |
| Current Figure 3 source column PNG | 0 | 0 | 5,175 | The source figure has no saturated red overlay/material pixels. |
| Current material-effect contact sheet | 0 | 0 | 25,095 | The regenerated selected material-effect panel is also free of strong-red Original-MDL cells. |

Follow-up layout diagnosis: the current user-visible defect was a two-column
layout split, not a material-rendering regression. The Figure 3 image had been
placed near the bottom of the left column while the `\captionof{figure}` text
could move to the top of the right column. The fix wraps the image, caption,
and label together in a single `\begin{minipage}{\columnwidth}` block in
`paper/venues/acl27/sections/limitations.tex`. Rendering page 9 after the fix
shows the image and caption together at the top of the right column, followed by
Ethical Considerations. The 180-DPI page-9 render is:

```text
tmp/acl27_current_visual_review_after_fig3_minipage_20260528/page-09.png
sha256=d1f855fe8be5a85d2f3bfb47cd04b1403ac9b9c43c0381472d4b72b98ebd110e
strong_red_pixels=0
redish_pixels=0
purple_overlay_like_pixels=15440
```

The refreshed 150-DPI full-PDF visual review contact sheet is:

```text
tmp/acl27_current_visual_review_final_20260528/contact_sheet.png
sha256=f5393b6106b35a28294eb25c317e24a8946eb31269304d29ec54b86ba3210a67
```

The material-effect clean-provenance checker remains clean:

```text
status=clean_material_effect_panel_ready
selected_case_count=4
complete_case_count=4
checked_original_mdl_log_count=4
missing_original_mdl_log_count=0
original_mdl_error_signal_count=0
blockers=[]
```

The repaired GRScenes scratch asset tree also still shows the expected import
state under
`/cpfs/user/zhuzihou/assets/zzh-grscenes_nomdl_full_work_20260521/scenes/GRScenes-100/home_scenes`:

```text
remaining "import ::KooPbr" / "import ::KooPbr_maps" matches: 0
repaired "using .::KooPbr" / "using .::KooPbr_maps" matches: 3683
```

Follow-up after the final Method/Fig.3 wording, minipage layout, and Figure 1
v15 imagegen passes: the PDF at `paper/venues/acl27/build/main.pdf` was rebuilt
and staged byte-identically with the SHA-256 and size above. The Figure 3
wording change replaced the main-text caption prefix from `Supplemental
qualitative` to `Selected qualitative`; the layout change keeps the image and
caption together in one single-column minipage. The source image remains
`paper/shared/figures/fig_internnav_rollout_0036_0066_column.png`. Rendering
page 9 at 180 DPI again reports `0` strong-red pixels, and the source column PNG
and retained full-width InternNav derivative both report `0` strong-red pixels.
The material-effect contact sheet is no longer blocked by stale original-MDL
logs: `check_qualitative_clean_provenance.py --report-only` reports
`status=clean_material_effect_panel_ready`, `checked_original_mdl_log_count=4`,
`original_mdl_error_signal_count=0`, and `blockers=[]`. Therefore the current
diagnosis is: the old red material was fixed at the asset/render-evidence
level, current Figure 3 is a clean InternNav orientation panel, and any PDF that
still shows an `Original MDL / ConvertAsset / NVIDIA` Figure 3 is an older
build or obsolete copy rather than the current `build/main.pdf`.

Current conclusion: the current ACL build path does not contain a red
`Original MDL` material Figure 3. If a viewer shows such a figure for this path,
it is showing an older PDF render/cache or a retired internal artifact. The
previous red-material root cause was stale pre-rerender original-MDL evidence;
the import repair did not retroactively fix old PNGs, so affected figures needed
fresh rerenders and provenance checks. The selected material-effect panel has
now been rerendered and passes that gate, while the ACL main PDF uses the
InternNav rollout panel as Figure 3.

## Follow-up Recheck: 2026-05-28 07:21 Build Bytes

The current bytes at the same user-reported path were rechecked again after the
abstract-boundary edit:

```text
paper/venues/acl27/build/main.pdf
sha256=b231f19b866b05923c04ed284efa3fc34e77b1b210b867b4cf44c2144cf41f68
size=3,073,082 bytes
pages=11
created=2026-05-28 07:21:43 CST
```

The diagnosis remains unchanged. `pdftotext` places Figure 3 on page 9 with the
caption `Selected qualitative InternNav rollout panel`, and `pdfimages -list`
shows the page-9 embedded image is `1050 x 718`, matching
`paper/shared/figures/fig_internnav_rollout_0036_0066_column.png`
(`sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5`).
Extracting the embedded page-9 image from the PDF and comparing it with that PNG
gives `nonzero_pixels=0`, so Figure 3 is exactly the repo source image.

Pixel spot checks for this exact build report:

| Artifact | Strong-red pixels | Red-ish pixels | Interpretation |
| --- | ---: | ---: | --- |
| Rendered PDF page 9 | 0 | 0 | No saturated red material signal in current Figure 3. |
| Current Figure 3 source column PNG | 0 | 4 | Tiny antialiasing/noise count only; not a material region. |
| Current material-effect contact sheet | 0 | 0 | The rerendered internal selected material-effect panel is clean. |

The material-effect provenance guard still reports
`status=clean_material_effect_panel_ready`, with
`checked_original_mdl_log_count=4`, `original_mdl_error_signal_count=0`, and
`blockers=[]`. Therefore the visible current Figure 3 colors are the intended
purple/green InternNav trajectory/reference overlays, not the old MDL fallback
red-material failure.

## Follow-up Recheck: 2026-05-28 07:28 Build Bytes

The same user-reported path was rechecked again after the latest abstract gate:

```text
paper/venues/acl27/build/main.pdf
sha256=021ad97eab2e78701ba8327750b28e36741446f3c5039396cc8c8dba572c48a6
size=3,073,082 bytes
pages=11
created=2026-05-28 07:28:12 CST
```

The PDF still embeds Figure 3 as the selected InternNav rollout panel, not the
retired material-effect contact sheet. `pdfimages -list -f 9 -l 9` reports one
embedded page-9 image of `1050 x 718`, matching the source figure:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Pixel comparison between the extracted embedded image and the source PNG reports
`nonzero_pixels=0`, so the PDF is using the current repo image bytes at the
rendered-pixel level.

Fresh pixel spot checks for this exact build report:

| Artifact | Strong-red pixels | Red-ish pixels | Purple-overlay-like pixels | Interpretation |
| --- | ---: | ---: | ---: | --- |
| Rendered PDF page 9 at 220 DPI | 0 | 0 | 756 | No saturated red material signal in the current Figure 3 page render. |
| Extracted page-9 embedded image | 0 | 0 | 2,051 | The embedded image has purple/green navigation overlays, not red material fallback. |
| Current Figure 3 source column PNG | 0 | 0 | 2,051 | The source figure remains free of saturated red pixels. |
| Current material-effect contact sheet | 0 | 0 | 0 | The regenerated internal selected material-effect panel is also clean. |

The underlying selected-video stills for path `597_597` do contain source-red
InternNav visualization overlays before paper composition: `475` original-start,
`4,177` original-end, `461` noMDL-start, and `3,003` noMDL-end pixels match the
agent-overlay recolor rule. Running
`paper/shared/figures/gen_internnav_main_readable.py` reports these `8,116`
source-red overlay pixels recolored to purple in the one-column Figure 3 panel.
This is a presentation-only overlay recolor; it does not edit material renders.

Scanning the corresponding InternNav selected-video log roots for `KooPbr`,
`KooPbr_maps`, `could not find module`, `Failed to create MDL shade node`,
`C108`, and `C120` returned no matches. The material-effect provenance guard
also still reports `status=clean_material_effect_panel_ready`,
`checked_original_mdl_log_count=4`, `original_mdl_error_signal_count=0`, and
`blockers=[]`.

Current diagnosis remains: the import repair fixed the old MDL-resolution
problem for fresh rerenders, but it could not retroactively change stale PNGs.
The current `main.pdf` at this path no longer contains the old
`Original MDL / ConvertAsset / NVIDIA` material-effect Figure 3. If a viewer
still shows red material in such a figure, it is showing an older PDF/cache or a
retired internal artifact rather than the current build bytes.

## Follow-up Recheck: 2026-05-28 10:17 Build Bytes

The user-reported path was rechecked again against the current ACL build:

```text
paper/venues/acl27/build/main.pdf
sha256=30b248b970aa586ce83f0c0f011d1576eee0af24b0eef6859124021bdc3b7e34
size=3,010,470 bytes
pages=11
created=2026-05-28 10:17:52 CST
```

The diagnosis remains unchanged. Figure 3 is on page 9 and is the selected
InternNav rollout panel, not the retired material-effect comparison panel.
`pdfimages -list -f 9 -l 9` reports one embedded page-9 image of `1050 x 718`,
matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Fresh pixel spot checks for this exact build report:

| Artifact | Strong-red pixels | Red/orange pixels | Purple pixels | Green pixels | Interpretation |
| --- | ---: | ---: | ---: | ---: | --- |
| Rendered PDF page 9 at 180 DPI | 0 | 5 | 642 | 8,136 | No saturated red material signal; visible colored marks are navigation overlays. |
| Current Figure 3 source column PNG | 0 | 0 | n/a | n/a | Source figure is free of saturated red pixels. |
| Current material-effect contact sheet | 0 | 0 | n/a | n/a | The rerendered internal selected material-effect panel remains clean. |

The source stills for the selected InternNav path still contain red pixels in
both original and noMDL conditions before paper composition, because InternNav
draws the executed trajectory/action arrow in red by default. The current figure
generator recolors those overlay pixels to purple in the paper figure; this is
why the source video stills can have red overlay pixels while the current PDF
Figure 3 has none.

Scanning the corresponding InternNav selected-video log roots for `KooPbr`,
`KooPbr_maps`, `could not find module`, `Failed to create MDL shade node`,
`C108`, and `C120` again returned no matches. The material-effect provenance
guard also still reports `status=clean_material_effect_panel_ready`,
`checked_original_mdl_log_count=4`, `original_mdl_error_signal_count=0`, and
`blockers=[]`.

Current conclusion: the old saturated-red material issue was a stale-render
artifact from the former material-effect panel. It has been fixed for fresh
material-effect evidence and is not present in the current `main.pdf` Figure 3.

## Follow-up Recheck: 2026-05-28 11:59 Build Bytes

The user-reported path was rechecked again after the latest local ACL build:

```text
paper/venues/acl27/build/main.pdf
sha256=4fb42c74a889f5ee2ad094305196d0fd2f62309549ad7579beb0d6c705484b06
size=3,010,349 bytes
pages=11
created=2026-05-28 11:59:21 CST
```

The diagnosis remains unchanged. `pdftotext -layout -f 9 -l 9` identifies
Figure 3 as:

```text
Figure 3: Selected qualitative InternNav rollout panel.
```

`pdfimages -list` reports that page 9 embeds a single `1050 x 718` RGB image.
Extracting that image with `pdfimages -png -f 9 -l 9` and comparing it against
the repository source figure gives `diff_bbox=None` and `nonzero_pixels=0`, so
the PDF embeds the exact current source image:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Fresh pixel spot checks for this exact build report:

| Artifact | Strong-red pixels | Red-ish pixels | Purple-overlay-like pixels | Interpretation |
| --- | ---: | ---: | ---: | --- |
| Rendered PDF page 9 at 180 DPI | 0 | 0 | 473 | No saturated red material signal in the current Figure 3 page render. |
| Current Figure 3 source column PNG | 0 | 0 | 1,939 | The source figure has purple navigation overlays, not red material fallback. |
| Current material-effect contact sheet | 0 | 0 | 0 | The rerendered selected material-effect panel also remains clean. |

The raw selected InternNav stills for path `597_597` do contain red navigation
overlay pixels before paper composition: `362` strong-red pixels in the original
start still, `3,361` in the original end still, `363` in the noMDL start still,
and `2,362` in the noMDL end still. These red pixels are the InternNav action
arrow and executed-trajectory overlay. The figure generator recolors the
overlay to purple in the paper-facing figure; rerunning
`paper/shared/figures/gen_internnav_main_readable.py` reports `8,116`
source-red overlay pixels recolored in the one-column Figure 3 panel.

Scanning the corresponding selected-video log roots for `KooPbr`,
`KooPbr_maps`, `could not find module`, `Failed to create MDL shade node`,
`C108`, and `C120` returned no matches. The material-effect clean-provenance
guard also reports `status=clean_material_effect_panel_ready`,
`checked_original_mdl_log_count=4`, `original_mdl_error_signal_count=0`, and
`blockers=[]`.

Current conclusion: if a viewer shows a red `Original MDL` material-effect
Figure 3 at this path, it is not showing the current file bytes. The current
PDF's Figure 3 is the InternNav rollout panel; the only red in its raw source
media is navigation-overlay annotation, and the paper-facing image has been
recolored to purple.

## Follow-up Recheck: 2026-05-28 12:58 Pre-upload Gate Build

The same user-reported path was rechecked after the consolidated pre-upload gate
rebuilt and staged the ACL candidate:

```text
paper/venues/acl27/build/main.pdf
sha256=b4d43ce170073bdc864354ba62c5056f92fae3272ae309b8a025c2ce145ad65b
size=3,009,971 bytes
pages=10
created=2026-05-28 12:58:38 CST
```

The current PDF still embeds Figure 3 from the InternNav column panel, not from
an `Original MDL / ConvertAsset / NVIDIA` material-effect contact sheet.
`pdfimages -list` reports a page-9 image of `1050 x 718`, and
`pdfimages -png -f 9 -l 9` extracts an image that is byte-equivalent at the
pixel level to:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
pixel-diff-nonzero=0
```

`pdftotext -layout -f 9 -l 9` places the caption on page 9 as:

```text
Figure 3: Selected qualitative InternNav rollout panel.
```

Fresh color checks for this exact gated build report:

| Artifact / cell | Strong-red pixels | Saturated-red pixels | Purple-overlay-like pixels | Interpretation |
| --- | ---: | ---: | ---: | --- |
| Rendered PDF page 9 at 180 DPI | 0 | 0 | 4,066 | No red material-fallback signal in the current rendered page. |
| Figure 3 source column PNG, whole image | 0 | 0 | 2,036 | The paper-facing figure uses purple navigation overlays. |
| Figure 3 source, original start cell | 0 | 0 | 108 | No red material signal in the original-MDL start cell. |
| Figure 3 source, original end cell | 0 | 0 | 932 | No red material signal in the original-MDL end cell. |
| Figure 3 source, noMDL start cell | 0 | 0 | 101 | No red material signal in the noMDL start cell. |
| Figure 3 source, noMDL end cell | 0 | 0 | 646 | No red material signal in the noMDL end cell. |
| Current material-effect contact sheet | 0 | 0 | 0 | The rerendered internal material-effect panel remains clean. |

The raw selected InternNav stills for the Figure 3 path still contain small
amounts of red before paper composition, but those pixels are concentrated in
InternNav's visualization overlays. For path `597_597`, the source stills report
`475`, `4,177`, `461`, and `3,003` strong-red pixels in original-start,
original-end, noMDL-start, and noMDL-end respectively. Most of those pixels are
in the lower top-down-map half; the upper RGB-view half contains only the
bottom-center action arrow. The paper figure generator recolors these red
overlay pixels to purple and leaves the source evidence stills unchanged.

The corresponding selected-video log roots still have no matches for `KooPbr`,
`KooPbr_maps`, `could not find module`, `Failed to create MDL shade node`,
`C108`, or `C120`. The material-effect provenance guard currently returns:

```text
ok=true
status=clean_material_effect_panel_ready
checked_original_mdl_log_count=4
original_mdl_error_signal_count=0
blockers=[]
```

Current conclusion: the March MDL import repair and the later clean rerenders
are not contradicted by the current Figure 3. If red `Original MDL` material is
visible to a viewer, the most likely explanations are a stale/cached PDF or a
different, retired material-effect figure asset. The current build-path PDF's
Figure 3 has zero strong-red/saturated-red pixels in its paper-facing image.

## Follow-up Recheck: 2026-05-28 13:28 Current Build

The same user-reported path was rechecked again after the current local ACL
build:

```text
paper/venues/acl27/build/main.pdf
sha256=f72294e50546948392ec74f2be3f26f7a0563b7aa69420dcb042eb57ef372d33
size=3,010,017 bytes
pages=10
created=2026-05-28 13:28:25 CST
```

The diagnosis is unchanged. `pdftotext` identifies Figure 3 as:

```text
Figure 3: Selected qualitative InternNav rollout panel.
```

`pdfimages -list` reports that the page-9 image is `1050 x 718`, matching the
paper source:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Extracting the embedded page-9 image and comparing pixels against that source
PNG gives:

```text
max_abs_diff=0
nonzero_pixels=0
```

Fresh color checks on this exact build report:

| Artifact | Strong-red pixels | Red-ish pixels | Red/orange pixels | Interpretation |
| --- | ---: | ---: | ---: | --- |
| Rendered PDF page 9 at 150 DPI | 0 | 0 | 0 | No red material-fallback signal in the current Figure 3 page render. |
| Figure 3 source column PNG | 0 | 0 | 0 | The paper-facing source figure contains purple navigation overlays, not red material. |
| Current material-effect contact sheet | 0 | 0 | 0 | The internal material-effect panel remains clean after rerender. |

The current ACL PDF embeds only three raster figures: Figure 1 on page 2,
Figure 2 on page 6, and Figure 3 on page 9. The page-9 raster is exactly the
InternNav rollout panel; the retired `Original MDL / ConvertAsset / NVIDIA`
material-effect contact sheet is not in the main PDF.

The material-effect provenance guard still reports:

```text
ok=true
status=clean_material_effect_panel_ready
checked_original_mdl_log_count=4
original_mdl_error_signal_count=0
blockers=[]
```

Current conclusion: the exact current `main.pdf` at the reported path does not
show the old abnormal red `Original MDL` material. The earlier red-material
problem was a stale original-MDL render/cache issue; the MDL import repair and
subsequent clean material-effect rerender are consistent with the current
evidence. If a viewer still shows a red material-effect Figure 3, it is likely
displaying a cached or older PDF, or a separate retired figure asset rather than
the current build-path PDF bytes.

## Follow-up Recheck: 2026-05-28 13:47 Current Build

The exact user-reported path was rechecked again after the current staged ACL
candidate build:

```text
paper/venues/acl27/build/main.pdf
sha256=4f04c57047e395e2afc86e0adbd247fcf257a9e730e03e0c34a4846521d62250
size=3,010,017 bytes
pages=10
created=2026-05-28 13:47:21 CST
```

The diagnosis remains unchanged. Figure 3 is the selected InternNav rollout
panel on page 9, not the retired `Original MDL / ConvertAsset / NVIDIA`
material-effect contact sheet. `pdfimages -list` reports three raster figures in
the PDF: Figure 1 on page 2, Figure 2 on page 6, and the page-9 Figure 3 image
with dimensions `1050 x 718`, matching:

```text
paper/shared/figures/fig_internnav_rollout_0036_0066_column.png
sha256=7ca4fdf99c648d740d64ad27e69fb44e4b6da20a98e58e51996163a65243dfe5
```

Fresh pixel checks on this exact build report:

| Artifact | Strong-red pixels | Interpretation |
| --- | ---: | --- |
| Rendered PDF page 9 at 220 DPI | 0 | No saturated-red material or overlay signal in the paper page render. |
| Figure 3 source column PNG | 0 | The paper-facing source uses purple/green navigation overlays. |
| Current material-effect contact sheet | 0 | The internal material-effect panel remains clean after rerender. |

The source InternNav video stills for the selected case still contain red
action arrows and red executed-trajectory strokes before paper composition. The
current generator recolors those source-red overlay pixels to purple while
leaving the stills themselves unchanged. For the single-column Figure 3 panel,
the generator reports `8116` source-red overlay pixels recolored across the
four start/end stills, including `4177` pixels in
`kujiale0036_0066_597_597_original_end_000113.png`. The same overlay pattern
also appears in noMDL stills, which is consistent with InternNav visualization
overlays rather than an original-MDL material fallback.

An MDL-error-term scan over the current selected-video InternNav log roots:

```text
/cpfs/user/zhuzihou/dev/InternNav/logs/official_kujiale0036_0066_original_video_selected6_20260525
/cpfs/user/zhuzihou/dev/InternNav/logs/official_kujiale0036_0066_nomdl_video_selected6_20260525
```

returned no matches for `KooPbr`, `KooPbr_maps`, `could not find module`,
`Failed to create MDL shade node`, `C108`, or `C120`.

Conclusion for this build: the current Figure 3 does not show the old abnormal
red `Original MDL` material. The only red in the underlying selected-video
source stills is InternNav's route/heading overlay before the paper panel
recolors it to purple. The old red-material problem was stale pre-repair render
evidence and is not present in the exact current PDF bytes.
